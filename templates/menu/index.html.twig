{% extends 'base.html.twig' %}

{% block title %} Menus du restaurant{% endblock %}

{% block body %}
    <h1> Les menus !</h1>
    {% for Menu in menuMethode %}
    {% for Formula in formulaMethode %}
		<div class="row">
			<div class="col-md-3 col-sm-3">
				{{Menu.title}}
				<div class="col-md-3 col-sm-3">
                {{Formula.title}}
				</div>
			</div>
		</div>   
    {% endfor %}
    {% endfor %}

{% endblock %}
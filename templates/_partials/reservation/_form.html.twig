{{ form_start(reservationForm) }}
<div class="row">
    <div class="col-md-6">{{ form_row(reservationForm.name)}}</div>
    <div class="col-md-6">{{ form_row(reservationForm.time)}}</div>
    <div class="col-md-6" id="guest">{{ form_row(reservationForm.guest)}}</div>
    <div class="col-md-6">{{ form_row(reservationForm.meal_allergy)}}</div>
    <div class="col-md-6">{{ form_row(reservationForm.is_service_full)}}</div>
    <button type="submit" class="btn btn-primary">{{ button_label|default('Réserver') }}</button>

</div>

{{ form_end(reservationForm) }}

{{ form_start(horaireForm) }}
<div class="row">
    <div class="col-md-6">{{ form_row(horaireForm.day)}}</div>
    <div class="col-md-6">{{ form_row(horaireForm.hour_in)}}</div>
    <div class="col-md-6">{{ form_row(horaireForm.hour_out)}}</div>
    <button type="submit" class="btn btn-primary">{{ button_label|default('Modifier') }}</button>

</div>

{{ form_end(horaireForm) }}